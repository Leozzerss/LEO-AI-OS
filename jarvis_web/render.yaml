services:
  - type: web
    name: leo-ai-mobile
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 8765
